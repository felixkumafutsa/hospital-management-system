{
    "version": 2,
    "outputDirectory": "apps/web/dist",
    "buildCommand": "npm install && cd apps/api && npm run build && cd ../web && npm run build",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/apps/api/dist/server.js"
        }
    ]
}