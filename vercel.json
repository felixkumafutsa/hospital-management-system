{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "installCommand": "npm install && cd apps/api && npm install && cd ../web && npm install",
    "buildCommand": "cd apps/api && npm run build && cd ../web && npm run build",
    "outputDirectory": "apps/web/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/apps/api/dist/server.js"
        }
    ]
}